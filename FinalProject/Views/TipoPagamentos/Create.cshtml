@model FinalProject.Models.Entity.TipoPagamento

@{
    ViewData["Title"] = "Criar";
}

<h1>Criar</h1>

<h4>TipoPagamento</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="nome_tpPag" class="control-label"></label>
                <input asp-for="nome_tpPag" class="form-control" />
                <span asp-validation-for="nome_tpPag" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Criar" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Voltar</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
