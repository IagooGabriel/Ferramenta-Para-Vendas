@model IEnumerable<FinalProject.Models.Entity.TipoPagamento>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Novo*</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.nome_tpPag)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.nome_tpPag)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.id_tpPag">Editar</a> |
                <a asp-action="Details" asp-route-id="@item.id_tpPag">Detalhes</a> |
                <a asp-action="Delete" asp-route-id="@item.id_tpPag">Apagar</a>
            </td>
        </tr>
}
    </tbody>
</table>
