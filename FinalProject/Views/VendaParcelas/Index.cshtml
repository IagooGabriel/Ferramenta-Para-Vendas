@model IEnumerable<FinalProject.Models.Entity.VendaParcela>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.parcelas)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.dt_vencimento)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.dt_pagamento)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.valor)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.parcelas)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.dt_vencimento)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.dt_pagamento)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.valor)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.id_parc">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.id_parc">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.id_parc">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
